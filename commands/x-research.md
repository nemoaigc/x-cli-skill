---
description: "Run a multi-source X/Twitter research brief"
argument-hint: "[topic/domain/time window]"
---

Use the `x` skill in research mode for:

`$ARGUMENTS`

Load `references/search-plan.md`, derive the domain and time window, probe before setting thresholds, combine follow-circle + keyword + trend + long-form/article sources as appropriate, then write a concise Markdown briefing with source links. If the request mentions papers, verify paper links outside X when possible.
